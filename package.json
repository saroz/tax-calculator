{
  "name": "second-batch",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.21.4",
    "sass": "^1.77.4"
  },
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded assets/scss:dist/css",
    "build": "esbuild assets/js/main.js --bundle --outfile=dist/js/index.js --loader:.html=text"
  }
}
